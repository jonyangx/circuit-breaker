# Normalize line endings: store LF in the repo, check out LF everywhere.
* text=auto eol=lf

# Windows-only scripts keep CRLF.
*.bat text eol=crlf
*.cmd text eol=crlf

# Binaries
*.jar binary
*.png binary
*.ico binary
